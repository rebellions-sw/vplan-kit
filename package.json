{
  "name": "vplan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Single-file verification plan editor (schema vplan/1.0)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "js-yaml": "^4.3.1"
  }
}
